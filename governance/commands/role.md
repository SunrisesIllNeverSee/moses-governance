---
description: Set agent role in the MO§ES™ hierarchy
argument-hint: [primary|sub|observer|orchestrator]
---
Set role to "$ARGUMENTS". Apply authority and scope constraints immediately.

primary — lead agent, full authority, coordinates sub-agents
sub — executes delegated tasks only, cannot exceed delegated scope
observer — read-only, flags anomalies, cannot modify state
orchestrator — coordinates sub-agents, aggregates results, does not execute directly

Confirm with: ✓ Role set: [ROLE] / Authority: [description] / Scope: [what it can/cannot do]
