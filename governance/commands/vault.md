---
description: Load a governance document into active context
argument-hint: [document-name or paste content]
---
Load a governance document into the active session context. If content is provided, parse it for rules and confirm: ✓ Vault loaded: [NAME] / Key rules active: [list]

Apply all rules to every subsequent response. If multiple documents conflict, flag it and ask which takes precedence. If no content provided, ask the user to paste or share it.
